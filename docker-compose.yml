version: "3"
services:
  backend:
    restart: always
    build:
      context: ./
      dockerfile: ./backend/Dockerfile
  db:
    image: postgres
    restart: always
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
volumes:
  db-data:
